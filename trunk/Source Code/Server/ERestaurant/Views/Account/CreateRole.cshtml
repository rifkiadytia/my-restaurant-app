
@model ERestaurant.Models.Role

<div class="accountHeader">
    <h2>
        Create a New Role</h2>
    <p>Use the form below to create a new Role.</p>
    </div>

@using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
    <label>@ViewBag.Message</label>
    
     @Html.DevExpress().Label(grSettings => {
        grSettings.Name = "RoleNameLabel";
        grSettings.Text = "Role Name";
        grSettings.AssociatedControlName = "RoleName";
    }).GetHtml()
    <div class="form-field">
        @Html.EditorFor(m => m.RoleName)
        @Html.ValidationMessageFor(m => m.RoleName)
    </div>

   @Html.DevExpress().Label(grSettings => {
        grSettings.Name = "RoleDescriptionLabel";
        grSettings.Text = "Role Description";
        grSettings.AssociatedControlName = "RoleDescription";
    }).GetHtml()
    <div class="form-field">
        @Html.TextAreaFor(m => m.RoleDescription)
        @Html.ValidationMessageFor(m => m.RoleDescription)
    </div>

    @Html.DevExpress().Button(grSettings => {
        grSettings.Name = "CreateRoleButton";
        grSettings.Text = "CreateRole";
        grSettings.UseSubmitBehavior = true;
     }).GetHtml()
}